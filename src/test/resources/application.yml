spring.application.name: service-docker-compose
spring:
  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect